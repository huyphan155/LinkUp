<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LinkUp Adventure" Height="450" Width="650"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" 
        Background="#0F2027">
    <Window.Resources>
        <!-- Neon Glow Button Style -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#1A1F27"/>
            <Setter Property="BorderBrush" Value="#00F0FF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#003E52"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00F0FF"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00F0FF" BlurRadius="15" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005B75"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#00C0CC"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00C0CC" BlurRadius="20" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#222833"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#555"/>
                                <Setter Property="Foreground" Value="#777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox item style with neon highlight -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#1A1F27"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter VerticalAlignment="Center" Margin="6,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#00F0FF"/>
                                <Setter Property="Foreground" Value="#0B1D1E"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00F0FF" BlurRadius="10" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#004D66"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="20" Background="#0F2027">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title with glow effect -->
        <TextBlock Text="⚔ LinkUp Adventure"
                   FontFamily="Consolas, Courier New, monospace"
                   FontSize="32" FontWeight="Bold"
                   Foreground="#00F0FF"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15">
            <TextBlock.Effect>
                <DropShadowEffect Color="#00F0FF" BlurRadius="10" ShadowDepth="0"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- Streak Label -->
        <TextBlock x:Name="StreakLabel" Grid.Row="1"
                   FontFamily="Consolas, Courier New, monospace"
                   FontSize="18" FontWeight="SemiBold"
                   Foreground="#00FFD6"
                   HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock.Effect>
                <DropShadowEffect Color="#00FFD6" BlurRadius="8" ShadowDepth="0"/>
            </TextBlock.Effect>
        </TextBlock>

        <!-- Config List -->
        <ListBox x:Name="ConfigList" Grid.Row="2"
                 BorderBrush="#00F0FF" BorderThickness="2"
                 Background="#10232F" Padding="5"/>

        <!-- Buttons Panel -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Center" Margin="0,25,0,0">
            <Button x:Name="LaunchButton" Content="🚀 Launch" Background="#00796B"/>
            <Button x:Name="PomodoroButton" Content="⏱ Pomodoro" Background="#0288D1"/>
            <Button x:Name="ExitButton" Content="❌ Exit" Background="#D32F2F"/>
        </StackPanel>
    </Grid>
</Window>
